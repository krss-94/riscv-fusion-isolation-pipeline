4.5 verified via tb_pipeline.cpp op_a_r debug dump against t_muldiv.bin — steady 0x64 across all MUL_RUN/DIV_RUN cycles
4.6a/b/c PASS after fixing fusion_active to also gate on flush_ex (was previously stale-lookahead-only, missed same-cycle branch/JALR/trap redirects racing the fusion decision). Bug found and fixed, not pre-existing-correct.
4.7 (adapted, no async interrupts in this core) PASS -- MUL completes fully during global freeze before illegal-instr trap taken; mepc=0x8000000c correctly points to trap PC, not mid-multiply.
4.8 PASS on first try -- all 8 sub-cases (ALU/branch/store-addr/2nd-MUL x EX-MEM/MEM-WB forward) correct, no bug found.
§2 gap closed: tb_muldiv.cpp only tested muldiv.sv (Stage 2 combinational unit), never muldiv_iter.sv (actual proposed-arch iterative unit). Added tb_muldiv_iter.cpp, same 14x8=112 golden vectors incl. INT_MIN/-1 and div-by-zero, driven through start/busy/done handshake. 112/112 PASS.
§6 HPM self-check: found+fixed testbench bug (pre-loop reset-deassert edge pulsed isol_active once, incrementing RTL counter before shadow count started) -- fusion/isol/muldiv all 0 mismatches after fix, across t_hpm_isol/t_fusion_idiom1_pos/t_fusion_muldiv_fwd_operand.
Full lockstep rerun (current RTL, post-Stage-6 edits): 92/92 PASS, 0 FAIL.
Standalone checks (HPM/CSR-reading tests, §6 scope boundary): 7/7 PASS, 0 FAIL.
Stage 6 (Part 10 §4 directed tests + §6 HPM self-consistency, idiom-1-only scope per RTL comment at core_top_pipelined.sv:109) CLOSED.
Stage 7 prep: found+fixed a real RTL bug in core_top_pipelined.sv -- UART_TXDATA (0x80000000) aliases MEM_BASE in the unified mem[] array with no address decode, so any UART store silently corrupted mem[0..3] (the reset-vector instruction). Added is_uart_txdata guard on the mem[] write (dbg_mem_we/dbg_mem_addr stay unconditional for tb_core.cpp's UART tap). Verified via standalone mem[0..3] before/after check on stage3_hello.bin (UNCHANGED after fix) and full regression: 92/92 lockstep + 7/7 standalone still PASS.
Also fixed tb_pipeline.cpp: stale flat rootp->core_top_pipelined__DOT__* signal access (pre-dates the nested-cell Verilator 5.050 layout already fixed in tb_hpm_selfcheck.cpp) -- rewrote all 32 accesses to rootp->core_top_pipelined->__PVT__* and added the corresponding nested header include. sim_pipeline now actually rebuilds and reflects current RTL; prior "PASS" results earlier in this session were against a stale pre-fix binary.
